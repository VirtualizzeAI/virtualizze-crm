import { PagePlaceholder } from '../components/shared'

export default function SettingsPage() {
  return (
    <PagePlaceholder
      title="Configurações"
      description="Reservado para preferências da organização, definição de campos customizados, usuários, integrações e parâmetros do sistema."
    />
  )
}