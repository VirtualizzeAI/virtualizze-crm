import { PagePlaceholder } from '../components/shared'

export default function ProductsPage() {
  return (
    <PagePlaceholder
      title="Produtos"
      description="Área placeholder para catálogo, estoque, promoções e uso dos produtos dentro de negócios e propostas comerciais."
    />
  )
}