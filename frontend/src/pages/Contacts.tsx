import { PagePlaceholder } from '../components/shared'

export default function ContactsPage() {
  return (
    <PagePlaceholder
      title="Contatos"
      description="Base para gestão de contatos PF e PJ, campos personalizados, notas, filtros e vínculo com negócios, tarefas e conversas."
    />
  )
}